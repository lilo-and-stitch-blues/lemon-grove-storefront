// Entry point for the Lemon Grove storefront service.
const { PRODUCTS } = require("./products");

console.log("Lemon Grove storefront booting...");
console.log(`Loaded ${PRODUCTS.length} products.`);
